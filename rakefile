#
# The documentation server build script.
#

require 'find'
require 'rake'
require 'rake/clean'

TMP_DIR = "/tmp"
HOME = ENV["HOME"]

# Load other rake tasks
Dir.glob('tools/*.rake') {|f| load(f) }
Dir.glob('tasks/*.rake') {|f| load(f) }
Dir.glob('configs/*.rake') {|f| load(f) }
